# 0005 — Newsletter backend design (module, tokens, rate limiting)

- **Status:** Accepted (design only — no implementation in this stage)
- **Date:** 2026-07-13
- **Stage:** 2C.1 (repository reconnaissance and implementation design)

## Context

Stage 2B shipped a coming-soon newsletter form behind a `NewsletterAdapter`
seam (`apps/storefront/src/lib/newsletter/index.ts`) that the dev adapter
satisfies by simulating success only when `NODE_ENV === "development"`
([0004](0004-coming-soon-ui-boundaries.md)). No subscriber persistence,
confirmation flow, email delivery, bot protection or rate limiting exists yet.
Stage 2C replaces that seam with a real Medusa-backed implementation. This
document records the design; no models, migrations, routes or dependencies
are added by it.

Repository facts this design relies on (see reconnaissance summary in the
Stage 2C.1 task report for full detail):

- Backend: `@medusajs/framework` / `@medusajs/medusa` 2.17.2, MikroORM
  (Postgres), Jest 29, no Redis, no custom modules/routes/workflows/jobs yet.
- Storefront: Next.js 15.5.18, React 19, Vitest, no validation library, no
  `resend`/reCAPTCHA packages installed.
- Database safety guard: `apps/backend/src/utils/assert-test-database.ts`
  requires the database name (parsed from `DATABASE_URL`) to match `/test/i`
  before integration tests may run against it.
- No shared logger, error-handling wrapper, or crypto utility exists in
  either app yet — Stage 2C introduces the first ones.

## Decisions

### Module structure

- **Module name:** `newsletter` (registration key `NEWSLETTER_MODULE`).
- **Directory:** `apps/backend/src/modules/newsletter/`
  - `models/subscriber.ts`, `models/rate-limit-bucket.ts`
  - `service.ts` — `NewsletterModuleService extends MedusaService({ Subscriber, RateLimitBucket })`, plus hand-written methods for token issuance/verification and rate-limit increment (the generated CRUD methods are not sufficient for hashed-token lookup or atomic counters).
  - `index.ts` — `export const NEWSLETTER_MODULE = "newsletter"; export default Module(NEWSLETTER_MODULE, { service: NewsletterModuleService })`.
  - `migrations/` — generated by the Medusa CLI, never hand-edited except for review.
- **Registration:** add `{ resolve: "./src/modules/newsletter" }` to the `modules` array in `apps/backend/medusa-config.ts` (currently absent — this is a one-line addition when implementation begins).
- **Access from routes/workflows:** `req.scope.resolve(NEWSLETTER_MODULE)` in API routes; workflow steps resolve the module inside `createStep` via the step's `container` argument. Domain logic (lifecycle transitions, token issuance, consent recording) lives in the module service and in workflows — API route files stay thin (parse input, call a workflow, map the result to a generic response).

### Data models

**`Subscriber`** (table `newsletter_subscriber`):

| field | type | notes |
|---|---|---|
| `id` | `model.id().primaryKey()` | Medusa-generated `text` PK (e.g. `sub_...`) |
| `first_name` | `model.text()` | |
| `email` | `model.text()` | original, as submitted (trimmed) |
| `normalised_email` | `model.text()` | see normalisation rule below; unique |
| `status` | `model.enum(["PENDING","CONFIRMED","UNSUBSCRIBED"])` | |
| `consent_text_version` | `model.text()` | |
| `consented_at` | `model.dateTime()` | |
| `source` | `model.text()` | e.g. `"coming-soon"` |
| `confirmation_token_hash` | `model.text().nullable()` | SHA-256 hex, unique when present |
| `confirmation_token_expires_at` | `model.dateTime().nullable()` | |
| `confirmed_at` | `model.dateTime().nullable()` | |
| `unsubscribe_token_hash` | `model.text().nullable()` | SHA-256 hex, unique when present |
| `unsubscribed_at` | `model.dateTime().nullable()` | |
| `first_purchase_discount_eligible` | `model.boolean().default(false)` | |
| `confirmation_email_last_sent_at` | `model.dateTime().nullable()` | **narrowly justified addition**: prevents unbounded resend loops when a `PENDING` address resubmits repeatedly (drives a short server-side cooldown, independent of the IP-based rate limiter, since a bad actor could rotate source IPs while reusing one victim email). |
| `confirmation_send_state` | `model.enum(["NOT_SENT","SENT","FAILED"]).default("NOT_SENT")` | **narrowly justified addition**: distinguishes "Resend accepted the send" from "token minted but delivery unconfirmed" so a later retry/cleanup job can find stuck rows without guessing from timestamps alone. |
| `created_at` / `updated_at` | auto | Medusa-generated |

`deleted_at` (Medusa's default soft-delete column) is **not used** for this
model — subscribers are never soft-deleted by this flow; only their `status`
changes. (Medusa still adds the column by default; it simply stays null.)

**`RateLimitBucket`** (table `newsletter_rate_limit_bucket`):

| field | type | notes |
|---|---|---|
| `id` | `model.id().primaryKey()` | |
| `request_key` | `model.text()` | HMAC-derived pseudonymous key, see below |
| `window_start` | `model.dateTime()` | start of the current fixed window |
| `attempt_count` | `model.number().default(0)` | |
| `created_at` / `updated_at` | auto | |

A fixed-window (not sliding) design is used deliberately: it needs only one
row per `(request_key, window_start)` and one atomic upsert, matching the
"no Redis, transactionally safe" constraint. `unique(["request_key",
"window_start"])` is declared on the model so `ON CONFLICT` can target it.

### Database constraints

- `subscriber.normalised_email`: `.unique()` — enforced by a unique index at
  the database level (not just an application check), so two concurrent
  `INSERT`s for the same normalised email cannot both succeed.
- `subscriber.confirmation_token_hash`: unique **partial** index (`WHERE
  confirmation_token_hash IS NOT NULL`) — most rows will have this null after
  confirmation/expiry, and Postgres unique indexes treat `NULL` as distinct
  from every other `NULL` by default, but an explicit partial index makes the
  intent clear and slightly cheaper.
- `subscriber.unsubscribe_token_hash`: unique partial index, same rationale.
- `rate_limit_bucket`: composite unique index on `(request_key,
  window_start)` — this is the conflict target for the atomic increment.
- All hash/token columns are `nullable`; all lifecycle timestamp columns are
  nullable except `consented_at` and `created_at`/`updated_at`.
- Indexes: `normalised_email` (implied by unique constraint), `status` (for
  future admin/reporting queries), `rate_limit_bucket(request_key,
  window_start)` (implied by unique constraint, doubles as the lookup index).

### Migrations

- Generate with `pnpm --filter @dtc/backend exec medusa db:generate
  newsletter` once the models exist — this produces a MikroORM migration file
  under `apps/backend/src/modules/newsletter/migrations/`, matching the
  Medusa v2 documented convention (module migrations live alongside the
  module, distinct from a repo-wide `migrations/` directory).
- Apply locally/in CI with the existing `medusa db:migrate` command
  (`apps/backend/package.json` already exposes this indirectly per
  `docs/operations/local-development.md`).
- **Safe test-database application**: the existing guard
  (`assert-test-database.ts`) must run before any migration command touches
  a database in an automated context; per CLAUDE.md, migrations are applied
  manually in review, and the guard's `/test/i` name check is the mechanical
  backstop against pointing at development/production by mistake. No change
  to the guard is proposed.
- **Rollback**: MikroORM migrations generated by Medusa include a `down()`;
  the standard `medusa db:rollback` reverses the last migration. Because this
  is a new module with no existing data, the practical rollback path during
  development is "fix the model, regenerate" rather than reversing partially
  applied migrations against real data.
- The migration itself is additive only (new tables) — no existing Medusa
  core tables are touched, so no interaction with existing data is expected.

### Lifecycle and concurrency

All transitions below are implemented as a single Medusa **workflow** per
public operation (`subscribeToNewsletterWorkflow`,
`confirmNewsletterSubscriptionWorkflow`, `unsubscribeFromNewsletterWorkflow`),
so that step failures roll back cleanly (e.g. a Resend failure after a row
was tentatively written must not leave the subscriber `CONFIRMED`).

| Scenario | Outcome |
|---|---|
| New signup | Row does not exist → insert `PENDING` with fresh consent + confirmation token; send confirmation email. |
| Repeated `PENDING` signup | Existing row found by `normalised_email`; token is **rotated** (old hash invalidated, new one issued) only if `confirmation_email_last_sent_at` is older than a cooldown (e.g. 5 minutes, config-driven); otherwise no-op. Response identical either way. |
| Concurrent first signup (two requests, same email, same instant) | Both attempt `INSERT ... ON CONFLICT (normalised_email) DO NOTHING`. Exactly one insert wins (Postgres guarantee, confirmed via official docs above); the loser re-reads the row and treats it as "repeated pending signup". No unique-constraint exception ever needs to propagate to the route. |
| Confirmed duplicate signup | Existing row has `status = CONFIRMED` → **no** row change, no email sent, same generic public response as a fresh signup. |
| Unsubscribed resubscription | Existing row has `status = UNSUBSCRIBED` → treated as a **new** signup for token/consent purposes: fresh `consent_text_version`/`consented_at`, fresh confirmation token, status set back to `PENDING`. This is an `UPDATE`, not a new row (normalised_email stays unique). |
| Confirmation | `UPDATE ... SET status='CONFIRMED', confirmed_at=now(), first_purchase_discount_eligible=true, confirmation_token_hash=NULL, confirmation_token_expires_at=NULL, unsubscribe_token_hash=COALESCE(unsubscribe_token_hash, <new hash>) WHERE confirmation_token_hash = $hash AND confirmation_token_expires_at > now() AND status = 'PENDING'` in one statement. Row-count 0 vs 1 distinguishes success from invalid/expired without a separate read-then-write race window. |
| Repeated confirmation | Second request's token hash no longer matches any row (already cleared) → treated as "already confirmed" (looked up by falling back to checking whether a `CONFIRMED` row exists that once matched — see Token design below for how this is resolved without keeping the plaintext token around) → idempotent generic success page. |
| Expired confirmation | `confirmation_token_expires_at <= now()` → update matches zero rows → "invalid or expired" result page; row itself is left as `PENDING` (a fresh signup can still rotate the token later). |
| Unsubscribe | `UPDATE ... SET status='UNSUBSCRIBED', unsubscribed_at=now() WHERE unsubscribe_token_hash = $hash AND status <> 'UNSUBSCRIBED'` — idempotent by construction; a second call matches zero rows and the route still reports generic success. |
| Repeated unsubscribe | Same statement, zero rows affected, same success result page. |
| Confirmation and unsubscribe racing | Both operate on the same row via single-statement conditional `UPDATE`s scoped by their own token hash and current `status`, so Postgres's row-level locking during the `UPDATE` serialises the two; whichever commits first determines the final `status`, and the loser's `WHERE` clause simply matches zero rows (e.g. if unsubscribe wins, a subsequent confirmation attempt no longer matches `status = 'PENDING'`). No explicit application-level lock is needed because each transition is a single conditional `UPDATE`, not read-then-write.

**Transaction boundaries:** each workflow's database-touching steps execute
inside one Medusa workflow transaction; the conditional single-statement
`UPDATE`/`INSERT ... ON CONFLICT` pattern above is what actually prevents
races (Postgres's own MVCC/row-locking), not application-level mutexes. The
Resend call is a separate, later, compensable step — see Resend section.

### Token design

- **Entropy:** `crypto.randomBytes(32)` (256 bits) for both confirmation and
  unsubscribe tokens.
- **Encoding:** base64url (no padding) for the value placed in URLs — shorter
  and URL-safe versus hex.
- **Hash algorithm:** SHA-256 of the raw token bytes, stored as hex in
  `*_token_hash`. SHA-256 (not bcrypt/argon2) is appropriate here because the
  input already has 256 bits of entropy — the risk being defended against is
  database compromise revealing usable tokens, not offline brute-force of a
  low-entropy secret, so a slow KDF adds cost without adding protection.
- **Lookup:** the incoming request's plaintext token is hashed and looked up
  by exact match on the unique `*_token_hash` index — this is why plaintext
  tokens are never stored: lookup and constant-time-equivalent comparison
  both happen implicitly via the unique index / `WHERE` clause, and a
  timing side-channel on "does this hash exist" leaks no more than "is this
  a valid token", which is already the observable outcome.
- **Expiry:** confirmation tokens expire after
  `NEWSLETTER_CONFIRMATION_TOKEN_TTL_MINUTES` (config-driven; enforced in the
  same conditional `UPDATE` as confirmation, not in a separate check).
  Unsubscribe tokens do not expire (a subscriber must always be able to
  leave the list).
- **Rotation:** a new confirmation token invalidates the previous one by
  overwriting `confirmation_token_hash` in the same row (old hash is
  simply no longer stored anywhere, so it stops matching).
- **Invalidation:** confirmation succeeding clears
  `confirmation_token_hash`/`confirmation_token_expires_at` to `NULL` so the
  same plaintext token cannot be reused to "re-confirm" via the confirmation
  endpoint (repeat requests fall through to the idempotent-success path
  instead, resolved by re-checking `status = 'CONFIRMED'` for that
  normalised email — the route never needs the old plaintext or old hash to
  answer "were you already confirmed?").
- **Logging protections:** the plaintext token appears only in the
  outbound confirmation/unsubscribe URLs (over TLS, in the Resend API
  payload) and in the incoming request's query string. It must never be
  passed to any logger; request-logging middleware (Section "Environment
  validation"/logging below) must redact the `token` query parameter by
  name before any structured log line is written.

### Rate limiting

- **Key derivation:** `HMAC-SHA256(NEWSLETTER_RATE_LIMIT_HASH_SECRET,
  clientAddress)`, hex-encoded, stored as `request_key`. This is what makes
  the design "no raw IP storage" — the secret is server-only, never exposed,
  and rotating it invalidates all existing buckets (acceptable, since buckets
  are short-lived).
- **Trusted request-address source:** the storefront calls the Medusa
  `/store/newsletter/subscribe` route directly from a Next.js **server**
  action/route handler (never straight from the browser), so Medusa sees the
  Vercel edge/Node runtime's own connection IP unless a proxy header is
  deliberately forwarded. Because the actual network path is
  browser → Vercel (Next.js server) → Medusa host, the design uses
  Next.js's own trusted `request.ip` (or the platform-provided client IP,
  e.g. Vercel's `x-vercel-ip-country`-style trusted header — mirroring how
  `middleware.ts` already trusts `x-vercel-ip-country` per the
  reconnaissance findings) on the storefront side, then forwards it to
  Medusa in a single custom header (e.g. `x-newsletter-client-key`) that
  Medusa trusts **only** from the storefront's own outbound call — arbitrary
  `X-Forwarded-For` values from the public internet are never trusted by the
  Medusa route directly, since Medusa is not itself internet-facing behind a
  trusted proxy in the current architecture. (This forwarding contract is a
  Stage 2C.2 implementation detail; flagged here as a risk to confirm against
  the actual Medusa hosting choice before coding — see Risks.)
- **Bucket structure:** fixed window, `(request_key, window_start)` where
  `window_start = floor(now / NEWSLETTER_RATE_LIMIT_WINDOW_SECONDS) *
  NEWSLETTER_RATE_LIMIT_WINDOW_SECONDS`, truncated to a `timestamptz`.
- **Atomic increment:** single statement —
  `INSERT INTO newsletter_rate_limit_bucket (request_key, window_start,
  attempt_count) VALUES ($1, $2, 1) ON CONFLICT (request_key, window_start)
  DO UPDATE SET attempt_count = newsletter_rate_limit_bucket.attempt_count +
  1 RETURNING attempt_count` — this is the documented atomic
  `ON CONFLICT DO UPDATE` guarantee, so concurrent requests from the same key
  cannot under-count (avoids the vulnerable read-then-write counter
  explicitly ruled out by the brief).
- **Limit decision:** if the returned `attempt_count >
  NEWSLETTER_RATE_LIMIT_MAX_REQUESTS`, the route returns the same generic
  public response used for every other rejected/duplicate case (never a
  distinct "rate limited" message, so the limiter itself isn't a fingerprint
  oracle) and skips the rest of the workflow (no email sent, no row
  touched).
- **Cleanup:** a Medusa scheduled job
  (`src/jobs/newsletter-rate-limit-cleanup.ts`, cron e.g. hourly) deletes
  buckets where `window_start < now() - <a few windows>`. Scheduled jobs run
  in-process per the documentation above and need no Redis, matching the
  "no Redis" constraint.
- **Indexes:** the `(request_key, window_start)` unique index doubles as
  both the conflict target and the lookup/cleanup index.
- **Failure behaviour:** if the rate-limit table write itself fails (e.g. DB
  unavailable), the design fails **closed** for the subscribe route — no
  database, no email send, matching "production must fail closed" language
  used for reCAPTCHA in the brief, applied consistently here for defence in
  depth.

### reCAPTCHA

- **Client boundary:** storefront calls `grecaptcha.execute(siteKey, {
  action: "newsletter_subscribe" })` **inside the form submit handler**, not
  in a `useEffect` on mount — matching the documented "call execute when the
  user takes the action, not on page load" guidance. The token is attached
  to the request body only, never logged, and is single-use (a fresh token
  is generated per submit attempt/retry).
- **Backend verification boundary:** the Medusa route (or a workflow step it
  calls) POSTs the token and `RECAPTCHA_SECRET_KEY` to Google's
  `siteverify` endpoint server-side; the secret never reaches the storefront
  bundle.
- **Action validation:** reject unless `response.action ===
  "newsletter_subscribe"`.
- **Score validation:** reject unless `response.score >=
  NEWSLETTER_RECAPTCHA_MIN_SCORE`.
- **Hostname handling:** reject unless `response.hostname` matches the
  expected storefront hostname(s) — this needs a small config value (a
  narrowly justified addition, e.g. `NEWSLETTER_RECAPTCHA_ALLOWED_HOSTNAMES`,
  a comma-separated list, since Preview deployments on Vercel get per-branch
  hostnames distinct from the production hostname).
- **Provider timeout handling:** the `siteverify` call uses a short timeout
  (e.g. 5s) with no retry inside the request path — a timeout or non-2xx is
  treated as verification failure.
- **Production fail-closed behaviour:** if `RECAPTCHA_SECRET_KEY` is unset,
  malformed, or `siteverify` is unreachable, the route rejects the
  submission with the same generic response whenever `NODE_ENV ===
  "production"`. A test-only bypass (e.g. a fixed test secret key that
  Google itself documents for automated testing, or an explicit
  `NEWSLETTER_RECAPTCHA_TEST_BYPASS` flag) must be structurally incapable of
  activating when `NODE_ENV === "production"` — the check is a hard `if
  (process.env.NODE_ENV === "production") { /* bypass code path physically
  absent */ }`, not a runtime flag alone, mirroring the CLAUDE.md
  requirement that this be "impossible to activate in production through an
  environment variable alone."
- **Test strategy:** unit-test the verification function against mocked
  `siteverify` responses (success/low-score/wrong-action/wrong-host/timeout/
  malformed-JSON); integration-test that the production build path has no
  bypass reachable via env vars alone.

### Resend

- **Adapter boundary:** a small `newsletter-email` sender module inside the
  `newsletter` Medusa module (e.g. `service.ts` calls out to a
  `resend-client.ts` helper), analogous to the storefront's existing
  `NewsletterAdapter` seam — the workflow step depends on an interface, not
  directly on the `resend` package, so it stays testable without network
  access.
- **Email rendering:** confirmation email has both `html` and `text` content
  generated from one shared template/data function (not two hand-maintained
  copies) so wording rules (no "already subscribed", no discount code, no
  urgency) are enforced in one place.
- **Country-aware URL generation:** the confirmation link is built from
  `PUBLIC_STOREFRONT_URL` plus the resolved country code (the same
  resolution the middleware already does, i.e. request-provided or
  configured default) plus `/newsletter/confirm?token=...` — no email
  address is embedded in the URL, only the opaque token.
- **Provider idempotency:** each send passes a Resend `Idempotency-Key`
  header derived deterministically from
  `subscriber.id + confirmation_token_hash` (stable for a given token, but
  different every time the token is rotated) — this uses Resend's own
  24-hour idempotency window as one layer of duplicate-send protection.
- **Application-level duplicate-send protection:** the
  `confirmation_email_last_sent_at` cooldown (see Data models) is the
  primary defence, since Resend's idempotency key only helps within its
  24-hour window and only for byte-identical requests; the cooldown check
  happens before the Resend call is even attempted.
- **Ambiguous-provider-result handling:** the Resend step wraps the SDK call
  and treats network timeout / 5xx / ambiguous response as "send unknown" —
  it sets `confirmation_send_state = "FAILED"` (not `"SENT"`) so the
  subscriber is **not** advanced to `CONFIRMED` on the strength of an
  unconfirmed send, and the workflow step's failure triggers the workflow's
  compensation logic to avoid leaving a half-applied state (e.g. a rotated
  token with no email actually delivered is still fine, since the public
  response is identical regardless of send success — but the send-state
  bookkeeping must reflect reality for later manual/automated retry).
- **Retry strategy:** Stage 2C.1 does not implement retries; the
  `confirmation_send_state` field exists so a future scheduled job (out of
  this stage's scope, flagged for 2C.2+) can find `FAILED`/`NOT_SENT` rows
  past a threshold and re-attempt. This stage sends synchronously once per
  request, matching "Stage 2C sends only a confirmation email."
- **Test strategy:** unit-test the adapter against a fake Resend client
  (success, 4xx, 5xx, timeout); integration-test that a Resend failure never
  results in `status = 'CONFIRMED'` and never leaks a provider error message
  to the public response.

### API routes

Files (all thin — parse/validate input, resolve the rate limiter and
reCAPTCHA checks, invoke a workflow, map result to a generic response):

- `apps/backend/src/api/store/newsletter/subscribe/route.ts` — `POST`.
  Order of checks: honeypot (silently generic-success if filled, no DB
  write) → rate limit → reCAPTCHA → input validation (first name, email,
  consent — reusing the same validation *rules* as the storefront's existing
  `apps/storefront/src/lib/newsletter/validation.ts`, ported/shared per
  "shared schemas rather than duplicate definitions"; exact sharing
  mechanism — a shared package vs. parallel implementations kept in sync by
  a shared spec/test — is a 2C.2 implementation decision) → invoke
  `subscribeToNewsletterWorkflow`. Always returns one generic `202`-style
  body regardless of new/pending/confirmed/unsubscribed outcome.
- `apps/backend/src/api/store/newsletter/confirm/route.ts` — `GET`. Accepts
  `?token=`, invokes `confirmNewsletterSubscriptionWorkflow`, redirects
  (`302`) to the storefront's country-aware result page with a coarse
  `?result=confirmed|invalid` query param (not the token itself).
- `apps/backend/src/api/store/newsletter/unsubscribe/route.ts` — `GET`,
  mirrors confirm.
- `apps/backend/src/api/middlewares.ts` — registers a body-size limit and
  (if Medusa's route-level validation middleware is used instead of manual
  parsing) a zod schema per route, following the documented
  `defineMiddlewares` pattern referenced in the generated
  `src/api/README.md`.
- The existing placeholder stubs `src/api/admin/custom/route.ts` and
  `src/api/store/custom/route.ts` are unrelated scaffolding and are not
  reused or modified by this design.

### Storefront integration

- **Exact existing adapter to replace:** the binding in
  `apps/storefront/src/lib/newsletter/index.ts`
  (`export const newsletterAdapter: NewsletterAdapter = devNewsletterAdapter`)
  is swapped for a new `api-adapter.ts` implementing the same
  `NewsletterAdapter` interface — the form component
  (`newsletter-form.tsx`) needs no changes. `dev-adapter.ts` can remain for
  local development without a running backend, selected via an existing or
  new feature flag (`NEWSLETTER_PULSE`-style flag per CLAUDE.md's feature
  flag guidance — exact flag name is a 2C.2 decision).
- **reCAPTCHA loading and submit-time execution:** the v3 script loads once
  (e.g. in a layout-level client component gated by
  `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` being present), and `grecaptcha.execute`
  is called from inside the adapter's `submit()` method — i.e. still only at
  submit time, not on mount.
- **Honeypot submission:** the form gains one additional hidden field (kept
  out of the tab order and visually hidden without `display:none`/`hidden`
  alone being the only defence — screen-reader-hidden via proper
  `aria-hidden`/off-screen technique) whose non-empty value is forwarded to
  the adapter and included in the POST body; CSS visibility is not treated
  as the security boundary — the backend check (silent generic success, no
  write) is.
- **Consent handling:** unchanged — the existing `validateConsent` boolean
  check stays; the adapter also sends `consentTextVersion` sourced from a
  storefront-side constant that must match `NEWSLETTER_CONSENT_TEXT_VERSION`
  on the backend (documented, not enforced by shared code, since the
  storefront doesn't currently read backend env — flagged in Risks).
- **Backend URL source:** the existing
  `NEXT_PUBLIC_MEDUSA_BACKEND_URL`/`getBaseURL()` convention already used
  elsewhere in the storefront (per `apps/storefront/src/lib/util/env.ts` and
  existing Medusa SDK usage) — no new public URL variable is needed for
  reaching Medusa itself.
- **Error and success UI:** unchanged generic success/error states already
  defined in `newsletter-form.tsx`'s `idle|submitting|success|error` model;
  the adapter maps every backend outcome (including rate-limited,
  reCAPTCHA-failed, honeypot-triggered) to the same `"success"` result per
  the "public responses must not reveal state" rule — failure is reserved
  for genuine transport/network errors only.

### Result pages

- **Exact proposed routes:**
  `apps/storefront/src/app/[countryCode]/newsletter/confirm/page.tsx` and
  `.../newsletter/unsubscribe/page.tsx`, following the same pattern already
  established for `coming-soon` and `privacy` (a `page.tsx` rendering a
  shared `*-view.tsx` component under `components/`, e.g.
  `components/newsletter/newsletter-confirm-view.tsx`).
- **Server vs. client responsibilities:** the page itself is a server
  component that reads the coarse `?result=` search param (never the raw
  token — the Medusa route already redirects with the token stripped) and
  passes a typed result-state prop to a presentational view component; no
  client-side fetch of subscriber state is needed or possible (public
  responses reveal nothing).
- **Token handling:** the storefront page never receives the token at all
  in this design — Medusa's `confirm`/`unsubscribe` routes do the
  token-bearing work server-side and redirect to a token-free URL. This
  satisfies "remove tokens from browser URLs after use" trivially (they're
  never present) and avoids referrer leakage from the result page itself.
- **Result-state mapping:** `confirmed | already-confirmed | invalid-or-expired`
  for confirm; `unsubscribed | already-unsubscribed | invalid` for
  unsubscribe — each mapped to accessible heading + body copy reusing
  `components/feedback` primitives already established in Stage 2A.
- **Design-system reuse:** headings use the existing Barlow Condensed
  display styles and `--ht-*` tokens; no new colour or component patterns
  are introduced.

### Environment validation

- **File ownership:** backend secrets live in `apps/backend/.env` (local)
  and `apps/backend/.env.template` (committed) — `RESEND_API_KEY`,
  `RESEND_FROM_EMAIL`, `RESEND_REPLY_TO_EMAIL`, `RECAPTCHA_SECRET_KEY`,
  `NEWSLETTER_RECAPTCHA_MIN_SCORE`,
  `NEWSLETTER_CONFIRMATION_TOKEN_TTL_MINUTES`,
  `NEWSLETTER_CONSENT_TEXT_VERSION`, `PUBLIC_STOREFRONT_URL`,
  `NEWSLETTER_RATE_LIMIT_WINDOW_SECONDS`,
  `NEWSLETTER_RATE_LIMIT_MAX_REQUESTS`,
  `NEWSLETTER_RATE_LIMIT_HASH_SECRET`. The storefront owns only
  `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` in `apps/storefront/.env.local` /
  `.env.template`.
- **Validation behaviour:** since no `process.env` schema validator exists
  yet in either app (confirmed by reconnaissance), Stage 2C introduces the
  **first** one for the backend — a small zod schema (zod is already a
  backend dependency, currently unused) validated at Medusa startup,
  throwing on missing/malformed required newsletter variables so
  misconfiguration fails at boot rather than on first request. This is a
  narrow, justified addition (not scope creep) because CLAUDE.md already
  requires "validate required variables when each application starts" and
  no prior stage established this — Stage 2C is the first stage to need
  backend secrets at all.
- **Fail-closed behaviour:** in `NODE_ENV === "production"`, a missing or
  malformed newsletter env var throws at startup (Medusa process does not
  boot) rather than degrading a specific route silently.

### Tests

- **Unit:** email normalisation; token generation/hash round-trip;
  rate-limit window/key derivation; consent parser (`"false"`/`"0"`/empty
  rejected); reCAPTCHA response validator (action/score/hostname); Resend
  payload/template builder (no discount code, no "already subscribed"
  wording, both html+text present).
- **Module/service tests** (`src/modules/newsletter/__tests__/*.spec.ts`,
  Jest `integration:modules` suite against the test database): subscriber
  lifecycle transitions table above, one test per row; unique-constraint
  behaviour on `normalised_email`.
- **Migration tests:** apply-then-inspect-schema smoke test (constraints and
  indexes exist as designed) run manually against the local test DB per the
  existing safety-guard workflow; no dedicated automated migration test
  framework exists in this repo today, so none is invented beyond what
  `db:migrate` + the module tests already exercise.
- **API integration tests** (`integration-tests/http/newsletter.spec.ts`,
  Jest `integration:http` suite): full subscribe → confirm → unsubscribe
  round trip against a real (test) database with the Resend/reCAPTCHA
  adapters swapped for fakes; generic-response assertion (same body shape
  for new/pending/confirmed/unsubscribed/rate-limited/honeypot-triggered).
- **Concurrency tests:** two simultaneous `subscribe` calls for the same
  email resolve to exactly one `PENDING` row; simultaneous confirm +
  unsubscribe against the same token pair resolve deterministically per the
  Lifecycle table.
- **reCAPTCHA tests:** verifier unit tests per the score/action/hostname
  matrix above; a dedicated test asserting the production bypass is
  structurally unreachable when `NODE_ENV === "production"`.
- **Resend adapter tests:** as above (success/4xx/5xx/timeout), plus a test
  that a failed send never results in `CONFIRMED`.
- **Storefront interaction tests** (Vitest): `api-adapter.ts` mapping of
  every backend response shape to `success`/`error`; honeypot field is
  submitted but not rendered visibly; reCAPTCHA `execute` is called only
  inside submit, never on mount (spy-based).
- **Middleware/country-route tests:** extend the existing
  `middleware.test.ts` pattern to confirm `/newsletter/confirm` and
  `/newsletter/unsubscribe` localise the same way `/coming-soon` does.
- **Test database safety checks:** all new integration/module tests run
  under the existing `assertTestDatabase` guard with no changes to it;
  explicitly verify (as a test) that running the newsletter module tests
  against a non-`*test*`-named `DATABASE_URL` throws before touching data.
- **Production-configuration failure tests:** backend boot throws when a
  required newsletter env var is missing in a simulated production
  environment (env schema validation above).

## Stage 2C.2 implementation notes

Stage 2C.2 implemented the persistence foundation described above almost
exactly as designed. Recorded here are the concrete outcomes and the one
place implementation diverged from the plan.

- **Module path:** `apps/backend/src/modules/newsletter/` — `index.ts`
  (`NEWSLETTER_MODULE = "newsletter"`), `service.ts`
  (`NewsletterModuleService extends MedusaService({ Subscriber,
  RateLimitBucket })` plus three lookup methods:
  `retrieveSubscriberByNormalisedEmail`,
  `retrieveSubscriberByConfirmationTokenHash`,
  `retrieveSubscriberByUnsubscribeTokenHash`), `models/subscriber.ts`,
  `models/rate-limit-bucket.ts`. Registered in
  `apps/backend/medusa-config.ts` via `modules: [{ resolve:
  "./src/modules/newsletter" }]`.
- **Tables:** `newsletter_subscriber`, `newsletter_rate_limit_bucket`
  (as designed).
- **Migration:** `apps/backend/src/modules/newsletter/migrations/Migration20260713170838.ts`,
  generated with `medusa db:generate newsletter` against the confirmed test
  database (`holotrail_medusa_test`) and applied with `medusa db:migrate`.
- **Indexes/constraints as generated by Medusa/MikroORM:** every unique
  index Medusa generates for a soft-deletable model (`normalised_email`,
  `confirmation_token_hash`, `unsubscribe_token_hash`, and the composite
  `(request_key, window_start)`) is automatically scoped
  `WHERE deleted_at IS NULL`. Because Postgres unique indexes already treat
  `NULL` as distinct from every other `NULL`, `.unique()` on a nullable
  column was sufficient on its own to satisfy "unique among non-null
  values, multiple nulls allowed" — **no manual partial-index workaround
  was needed**, simpler than the 2C.1 plan anticipated. Bounded string
  lengths (`first_name` ≤ 100, `email`/`normalised_email` ≤ 254,
  `consent_text_version` ≤ 32, `source` ≤ 64) and the non-negative
  `count` constraint were added as explicit, named Postgres `CHECK`
  constraints via the DML's `.checks()` method (the DML has no native
  bounded-string type in this Medusa version).
- **One manual correction during generation:** the first generation attempt
  used the `.checks([(columns) => "..."])` function-array form, which
  Medusa/MikroORM names identically for every entry (`<table>_check`),
  producing five colliding constraint names on `newsletter_subscriber`. The
  guarded `db:migrate` run against the test database failed with `check
  constraint "newsletter_subscriber_check" already exists`, and rolled back
  cleanly (verified directly against the test database: zero
  `newsletter_*` tables and no migration row remained). The fix was to use
  the object form (`{ name, expression }`) to give each check constraint an
  explicit, stable name (`CK_newsletter_subscriber_<field>_length`,
  `CK_newsletter_rate_limit_bucket_count_non_negative`); the migration was
  regenerated and applied successfully. No other adjustment was needed.
- **Test migration command:** `medusa db:generate newsletter` then `medusa
  db:migrate`, both invoked through a one-off guarded script that loads the
  `test` environment the same way `integration-tests/setup.js` does, calls
  the existing `assertTestDatabase` guard, and prints a redacted
  `host`/`database` summary before running the Medusa CLI command. The
  script was not committed (temporary, deleted after use); the guard logic
  it calls is the repository's existing, unmodified
  `assert-test-database.ts`.
- **Test-database target verification:** confirmed twice — once via the
  guard script's `assertTestDatabase` call before generation/migration, and
  again by querying `information_schema.tables`,
  `pg_constraint`/`pg_indexes`, and `mikro_orm_migrations` directly against
  the same `DATABASE_URL` after migrating, using the `pg` package already
  present as a transitive MikroORM dependency.
- **Module/service tests:** `apps/backend/src/modules/newsletter/__tests__/newsletter-module.spec.ts`,
  17 cases, run via `TEST_TYPE=integration:modules`. These bootstrap the
  module directly with `@medusajs/framework/modules-sdk`'s `MedusaApp` and
  `createPgConnection` (the same primitives `@medusajs/test-utils`'s
  `initModules` wraps) rather than importing `@medusajs/test-utils`
  itself — that package's `database` helper unconditionally requires the
  `pg-god` package (used for out-of-band database create/drop against
  separate `DB_HOST`/`DB_USERNAME`/`DB_PORT` env vars, not the
  already-guarded `DATABASE_URL`), and `pg-god` is not an installed
  dependency in this repository. Bootstrapping directly avoids both the
  missing dependency and a second, ungoverned database-lifecycle mechanism
  running alongside the existing safety guard. Test rows created during the
  run were deleted afterwards with a table-scoped `DELETE` against the
  same confirmed test database (no schema/database drop).
- **Deviation summary:** functionally identical to the 2C.1 design; the
  only differences are (1) no manual partial-index migration adjustment was
  needed (Medusa's default soft-delete-aware unique indexes already provide
  it), and (2) `@medusajs/test-utils` was not used for the module test
  runner due to its undeclared `pg-god` dependency — a direct
  `MedusaApp`/`createPgConnection` bootstrap was used instead.

## Stage 2C.3 implementation notes

Stage 2C.3 implemented the subscriber lifecycle and token-security layer
directly on `NewsletterModuleService`, not as Medusa workflows. Recorded
here are the concrete outcomes, the one schema addition, and where this
diverges from the 2C.1 design.

- **Structure decision:** the 2C.1 design proposed one workflow per public
  operation (`subscribeToNewsletterWorkflow`, etc.) specifically so a
  Resend failure after a tentative write could trigger workflow
  compensation. Stage 2C.3 has no public routes and no Resend call yet —
  only the pure database lifecycle — so three transactional methods
  (`prepareSubscription`, `confirmSubscription`, `unsubscribeSubscription`)
  were added straight to `NewsletterModuleService` instead. This is the
  smallest structure that still gives real transaction boundaries and
  atomicity; introducing workflow scaffolding now, before there is any
  compensable multi-step side effect to orchestrate, would be premature
  abstraction. Workflows remain the right choice once a later stage adds
  the Resend step — flagged here, not implemented.
- **Raw-SQL transaction pattern:** each lifecycle method opens one
  transaction via `this.manager_.transactional(...)` and issues
  parameterised `manager.execute(sql, params)` calls (single conditional
  `UPDATE`/`INSERT ... ON CONFLICT DO NOTHING`, plus `SELECT ... FOR
  UPDATE` row locks where a decision has to be made before mutating). The
  generated `MedusaService` CRUD methods cannot express these
  single-statement conditional writes, so `container.manager` (the
  module's own MikroORM `SqlEntityManager`, already injected by the
  standard SQL-module connection loader) is used directly — this is the
  same, official pattern `@medusajs/locking-postgres`'s
  `PostgresAdvisoryLockProvider` uses (`container.manager`, `manager.execute`,
  `manager.transactional`), not an invented one. A narrow local
  `NewsletterEntityManager`/`NewsletterTransactionManager` interface
  isolates the handful of methods actually called, instead of typing
  `container.manager` as `any`.
- **Email normalisation:** `lifecycle/email.ts` — trim, reject empty/over
  254 chars, and lower-case the *entire* address (local part and domain)
  for `normalisedEmail`. The *display* `email` keeps original casing
  (whitespace-trimmed only). Deliberately does not strip dots or `+`
  aliases and applies no Gmail-specific behaviour, per the brief; not an
  attempt at full RFC 5321 mailbox canonicalisation.
- **Token generation:** `lifecycle/token.ts` — `randomBytes(32)` (256 bits)
  base64url-encoded (`generateOpaqueToken`), SHA-256 hex digest for storage
  (`hashToken`), no salt (justified: the input already carries 256 bits of
  entropy). No password-hashing dependency was added.
- **Confirmation TTL:** `lifecycle/config.ts` —
  `resolveConfirmationTokenTtlMinutes(overrideMinutes?)` reads
  `NEWSLETTER_CONFIRMATION_TOKEN_TTL_MINUTES`, defaults to 60 minutes if
  unset, rejects non-finite/non-integer/zero/negative/values over 10,080
  minutes (7 days). The override parameter exists only for deterministic
  tests; production always resolves from the environment or the default.
  Added to `apps/backend/.env.template` only (commented out, backend-only,
  never on the storefront) — this is a single narrow reader, not the full
  Stage 2C environment schema (still deferred to Stage 2C.8).
- **Schema addition — `confirmation_token_consumed_hash`:** the 2C.1/2C.2
  schema could not correctly support idempotent repeated confirmation.
  Confirmation clears the active `confirmation_token_hash` on success (so
  the token cannot be reused to re-confirm or kept alive indefinitely), but
  that alone makes a repeated click on the same link indistinguishable from
  an arbitrary invalid token — both fail to match any row. A new nullable
  `confirmation_token_consumed_hash` column (unique when non-null, indexed
  the same way as the other token-hash columns —
  `IDX_newsletter_subscriber_confirmation_token_consumed_hash`, `WHERE
  deleted_at IS NULL`) records the hash of whichever token actually
  confirmed the subscriber. A confirmation lookup matches on
  `confirmation_token_hash OR confirmation_token_consumed_hash`; if only
  the consumed-hash column matches, the subscriber was already confirmed
  with that exact token and the method returns `ALREADY_CONFIRMED` (a
  no-op); if neither matches, the token is treated as invalid. The
  `PENDING_REFRESHED` and unsubscribed-resubscription transitions clear
  `confirmation_token_consumed_hash` back to `null`, since a freshly
  (re)issued token has nothing to be "already confirmed" against yet. This
  is an additive-only migration
  (`Migration20260713175132.ts`), generated and applied against the same
  confirmed test database as Stage 2C.2, following the same guarded
  one-off script (loads the `test` environment, calls the existing,
  unmodified `assertTestDatabase` guard, prints a redacted target summary,
  not committed). No other schema change was required.
- **Unsubscribe-token idempotency:** no schema addition was needed here —
  unlike the confirmation token, the unsubscribe token is never cleared on
  use, so a repeated unsubscribe request naturally finds the same row via
  the same hash and simply returns `ALREADY_UNSUBSCRIBED` without a second
  write.
- **Confirmation/unsubscribe race:** by construction, a subscriber can only
  acquire a valid `unsubscribe_token_hash` *after* it is `CONFIRMED`, and
  `prepareSubscription`'s unsubscribed-resubscription path always nulls the
  old `unsubscribe_token_hash`. This means the window the 2C.1 design
  worried about (a stray confirmation racing a valid unsubscribe on the
  same row) cannot arise from a still-`PENDING` row — confirmation is a
  one-shot, self-invalidating transition. The realistic race is a
  *replayed* confirmation (the original, now-consumed token) arriving
  concurrently with a valid unsubscribe; `newsletter-lifecycle.spec.ts`
  exercises this directly with `Promise.all([unsubscribe(...),
  confirm(originalToken)])` and asserts the subscriber never ends up
  `CONFIRMED` with `unsubscribed_at` populated, and never `UNSUBSCRIBED`
  with `first_purchase_discount_eligible` true. Safety comes from each
  method's conditional `UPDATE ... WHERE status = '<expected>' AND
  <token column> = ?` plus `SELECT ... FOR UPDATE` row locking, not from an
  application-level mutex.
- **Internal outcome types:** `lifecycle/types.ts` —
  `SignupResult` (`PENDING_CREATED | PENDING_REFRESHED | ALREADY_CONFIRMED`),
  `ConfirmationResult` (`CONFIRMED | ALREADY_CONFIRMED | INVALID_OR_EXPIRED`),
  `UnsubscribeResult` (`UNSUBSCRIBED | ALREADY_UNSUBSCRIBED | INVALID`). An
  unsubscribed-resubscription resolves to `PENDING_CREATED` (it behaves
  identically to a new signup from the caller's perspective — a
  confirmation email must be sent) rather than inventing a fourth outcome,
  per the brief's "keep the set minimal" instruction.
- **Tests added:**
  `src/modules/newsletter/__tests__/email.unit.spec.ts`,
  `token.unit.spec.ts`, `clean-input.unit.spec.ts`, `config.unit.spec.ts`
  (pure unit tests, `TEST_TYPE=unit`), and new lifecycle/concurrency
  `describe` blocks appended to the existing
  `newsletter-module.spec.ts` (DB-backed, `TEST_TYPE=integration:modules`)
  covering every transition in the table above plus the concurrency/race
  scenarios. A separate `newsletter-lifecycle.spec.ts` file with its own
  `MedusaApp` bootstrap was tried first and reverted: two spec files in the
  same `--runInBand` Jest worker each bootstrapping the `newsletter` module
  key corrupts `MedusaModule`'s process-wide static registry (`Method
  Map.prototype.set called on incompatible receiver`), even with a full
  `onApplicationShutdown()` + `MedusaModule.clearInstances()` in each
  file's `afterAll`. Sharing one bootstrap per Jest worker avoids the
  problem entirely.
- **Deviation summary:** (1) direct transactional module-service methods
  instead of workflows, deferred until a compensable side effect (Resend)
  exists to orchestrate; (2) one additive schema field
  (`confirmation_token_consumed_hash`) not anticipated by the 2C.1 design,
  required to make repeated-confirmation idempotency actually distinguish
  a consumed valid token from an arbitrary invalid one; (3) the
  confirmation/unsubscribe race is structurally narrower than the 2C.1
  design assumed, because the unsubscribe token cannot exist before
  confirmation and is always invalidated on resubscription.

## Stage 2C.4 implementation notes

Stage 2C.4 implemented the internal rate-limiting and reCAPTCHA
verification boundaries described in the "Rate limiting" and "reCAPTCHA"
sections above, as standalone, narrowly-scoped units with no public route
consuming them yet. Recorded here are the concrete outcomes and the places
implementation diverged from the 2C.1 design.

- **Module layout:** `apps/backend/src/modules/newsletter/rate-limit/`
  (`config.ts`, `request-key.ts`, `client-address.ts`, `window.ts`,
  `types.ts`, `rate-limiter.ts`, `cleanup.ts`) and
  `apps/backend/src/modules/newsletter/recaptcha/` (`config.ts`,
  `verify.ts`), plus a small shared `apps/backend/src/modules/newsletter/shared/env-parsing.ts`
  (zod building blocks reused by both config readers, so the "required,
  bounded, numeric-string" parsing pattern is written once). Each concern
  (config parsing, key derivation, address extraction, window math,
  database increment, cleanup, provider verification) is its own file,
  independently unit-testable, per the brief's "keep these separated
  enough to test directly."
- **Configuration:** `resolveRateLimitConfig` and `resolveRecaptchaConfig`
  both take an optional `env` parameter (defaulting to `process.env`) and
  always throw a `MedusaError` on missing or invalid required input, in
  every environment — there is no `NODE_ENV`-specific branch or default.
  This is what makes "production fails closed on missing configuration"
  true by construction: nothing downstream can be reached without a
  successful call to one of these functions. Documented bounds: rate-limit
  window 1–86,400 seconds; max requests 1–1,000; hash secret ≥32
  characters; reCAPTCHA min score 0.0–1.0; max token age 1–300 seconds
  (default 120, matching Google's documented ~2-minute token lifetime).
- **Request-key derivation:** `deriveRateLimitRequestKey` —
  `HMAC-SHA256(hashSecret, clientAddress)`, hex-encoded, 64 characters
  (full digest, never truncated), exactly as designed in 2C.1.
- **Client-address extraction:** `client-address.ts` implements the
  narrow, non-generic trust model the 2C.1 "Risks" section flagged as
  unresolved. Because the Medusa production host is still undecided, the
  implementation does **not** assume the storefront-forwards-a-custom-header
  design sketched in 2C.1; instead it exposes two independently testable
  primitives — `resolveTrustedProxyConfig` (reads
  `NEWSLETTER_TRUST_PROXY`/`NEWSLETTER_TRUSTED_IP_HEADER`) and
  `extractClientAddress` (given a `{ socketRemoteAddress, headers }`
  context and that config, returns a validated address or a typed failure)
  — so whichever forwarding contract the eventual host requires can be
  wired in later without changing the trust logic itself. By default *no*
  header is trusted; enabling header trust requires both env vars set
  together (a half-configured trust mode throws rather than silently
  falling back). A configured header must carry exactly one value — an
  array (repeated header) or a comma-separated chain (the classic
  `X-Forwarded-For` shape) is rejected outright, since no multi-hop trust
  boundary is defined. Addresses are validated with Node's built-in
  `net.isIP` (no new dependency); IPv4-mapped IPv6, bracket/port and
  zone-id forms are normalised before validation. **Deferred to whoever
  finalises the Medusa host:** confirm the actual header name Medusa will
  see behind that host's proxy (or confirm no proxy sits in front of
  Medusa at all, in which case `NEWSLETTER_TRUST_PROXY` stays `false` and
  the direct socket address is used) and set
  `NEWSLETTER_TRUST_PROXY`/`NEWSLETTER_TRUSTED_IP_HEADER` accordingly.
- **Fixed window:** `resolveRateLimitWindow` floors epoch seconds (not
  local calendar time) to a multiple of the configured window length,
  exactly as designed — no timezone or DST dependence, deterministic given
  an injected `now`.
- **Atomic increment:** added directly to `NewsletterModuleService`
  (`incrementRateLimitBucket`), using the same raw-SQL
  `container.manager`/`manager.execute` pattern Stage 2C.3 established for
  the subscriber lifecycle (not a separate service) — single statement:
  `INSERT ... ON CONFLICT (request_key, window_start) WHERE deleted_at IS
  NULL DO UPDATE SET count = count + 1 RETURNING count`. The `WHERE
  deleted_at IS NULL` conflict-target clause is required because Medusa
  generated the underlying unique index as a **partial** index scoped that
  way (confirmed against `Migration20260713170838.ts`); Postgres requires
  the `ON CONFLICT` target to match a partial index's predicate exactly
  for conflict inference. No schema change was needed — the Stage 2C.2/2C.3
  `RateLimitBucket` model already had everything this statement requires.
- **Threshold semantics:** a resulting count `<= maxRequests` is allowed; a
  count strictly greater than `maxRequests` is denied. The bucket is never
  deleted on denial — it simply resets naturally in the next window.
- **Orchestration:** `rate-limit/rate-limiter.ts` exports a standalone
  `checkRateLimit({ store, clientAddress, config, now? })` function rather
  than adding this to `NewsletterModuleService` directly — `store` is a
  one-method interface (`incrementRateLimitBucket`) satisfied by the
  module service in production and by an in-memory fake in unit tests.
  This keeps the full allow/deny decision testable without a database.
  Every internal failure path (HMAC derivation error, database error) is
  caught and converted into `{ allowed: false, reason: ... }` — this
  function can never resolve with `allowed: true` on a failure.
- **Internal result type:** `RateLimitOutcome` carries only `allowed`,
  `limit`, `remaining` (clamped to zero), `retryAfterSeconds`, `windowEndsAt`,
  and (only on denial) an internal `reason` — never the raw address, the
  HMAC secret, or the request key.
- **Cleanup:** `NewsletterModuleService.cleanupExpiredRateLimitBuckets(cutoff,
  batchSize = 10_000)` — a bounded `DELETE ... WHERE id IN (SELECT id ...
  WHERE window_start < cutoff ... LIMIT batchSize)` against the indexed
  `window_start` column, idempotent (a repeated call with the same cutoff
  deletes zero further rows). `src/jobs/newsletter-rate-limit-cleanup.ts`
  is a Medusa scheduled job (`schedule: "0 * * * *"`, hourly) that computes
  the cutoff via the pure, independently-tested
  `computeRateLimitCleanupCutoff(now, windowSeconds, retentionWindows)`
  (retention = 3 completed windows, so the cutoff is always strictly
  before the active window's `windowStart` and the active bucket is never
  touched) and logs only the aggregate deleted-row count — never a request
  key or bucket id.
- **reCAPTCHA verification endpoint:** `GoogleRecaptchaVerifier` (implements
  a small `RecaptchaVerifier` interface) POSTs `secret` and `response`
  (the token) to `https://www.google.com/recaptcha/api/siteverify` as
  `application/x-www-form-urlencoded`, via the runtime's global `fetch`
  (injectable for tests, no new HTTP dependency). `remoteip` is
  deliberately never sent — per the brief, the project's "avoid raw-IP
  handling where practical" rule outweighs the marginal signal it would
  add. Bounded 5-second timeout via `AbortController`; a timeout, network
  failure, non-2xx response, or malformed JSON all resolve to
  `{ verified: false, reason: "PROVIDER_ERROR" | "INVALID_RESPONSE" }`
  (fail closed), never a thrown exception.
- **Checks, in the order specified by the brief:** empty/missing token
  (short-circuits before any network call) → provider JSON parses →
  `success === true` (provider `error-codes` mapped to
  `MISCONFIGURED`/`EXPIRED_TOKEN`/`MISSING_TOKEN`/`INVALID_RESPONSE`, else
  `PROVIDER_ERROR`) → `action === "newsletter_subscribe"` (hard-coded
  constant, not a caller-supplied parameter — a compromised caller must
  not be able to widen what counts as a valid action) → `score` is a
  finite number in `[0, 1]` and `>= minScore` (equal to the threshold
  passes) → hostname, only when `allowedHostnames` is configured →
  `challenge_ts` present, parseable, and within
  `[-5s tolerance, +maxTokenAgeSeconds]` of now (a missing or malformed
  timestamp is treated as `EXPIRED_TOKEN`, never as valid, since it would
  otherwise skip age verification entirely).
- **Result type:** `RecaptchaVerificationResult` is `{ verified: true }` or
  `{ verified: false; reason: RecaptchaVerificationReason }` — the eight
  reasons listed in the brief, nothing else. The token, the secret, and
  the full provider response body are never included in the result, never
  thrown in an exception, and never logged (`verify.ts` contains no
  logging calls at all).
- **Test-only bypass:** no bypass code exists inside `GoogleRecaptchaVerifier` —
  it does not read `process.env`, does not branch on `NODE_ENV`, and does
  not special-case any token string. `RecaptchaVerifier` is an interface;
  automated tests requiring a fake verifier define one locally inside the
  test file itself
  (`recaptcha/__tests__/bypass.unit.spec.ts`'s `FakeRecaptchaVerifier`),
  never exported from `src/`, so there is no bypass symbol a production
  route could accidentally import. `bypass.unit.spec.ts` additionally
  proves, behaviourally, that setting `NODE_ENV=production` together with
  env vars that *look* like bypass flags (`RECAPTCHA_BYPASS`,
  `NEWSLETTER_RECAPTCHA_TEST_BYPASS`) does not change `GoogleRecaptchaVerifier`'s
  behaviour, and that no "magic" token string short-circuits verification.
- **Schema:** no migration was required — the Stage 2C.2 `RateLimitBucket`
  model (`request_key`, `window_start`, `count`, plus the composite unique
  index) already supports every operation this stage adds.
- **Deviation summary:** (1) the 2C.1 design's specific
  "storefront-computes-a-trusted-header, Medusa-trusts-it-unconditionally"
  contract was generalised into a configurable, off-by-default
  `NEWSLETTER_TRUST_PROXY`/`NEWSLETTER_TRUSTED_IP_HEADER` pair, because the
  Medusa host (and therefore the real proxy topology) is still undecided —
  flagged in 2C.1's own "Risks" section as needing confirmation before
  coding, so this is the conservative abstraction that section asked for,
  not a rejection of the original idea; (2) the atomic increment and
  cleanup methods were added directly to `NewsletterModuleService`
  (mirroring the 2C.3 precedent of raw-SQL methods on the module service)
  rather than a separate internal service, since no second bounded context
  emerged that would justify one; (3) the rate-limit allow/deny decision
  itself (`checkRateLimit`) was kept as a standalone function taking an
  injected store interface, rather than a service method, specifically so
  it stays testable without a database — this is a narrower version of the
  same "keep the pure decision logic separate from the persistence call"
  pattern used throughout this stage.

## Consequences

- Stage 2C.2+ implements this design: models, migrations, module service,
  workflows, routes, storefront adapter, result pages, and the tests above.
- The client-IP-forwarding-header contract between the storefront's server
  runtime and the Medusa host (see Rate limiting) must be confirmed against
  the actual Medusa hosting choice before coding — flagged as a risk below,
  not resolved by this document.
- The `consentTextVersion` sync between storefront constant and backend env
  var is documentation-enforced, not code-enforced; a future stage could
  move it to a single shared source if drift becomes a problem.
- This is the first stage to add: a Postgres env-schema validator, a
  workflow-based subscriber lifecycle, a scheduled job, and a token/crypto
  utility — all currently absent from the codebase per reconnaissance.

## Risks or unresolved conflicts

- **Client-address trust source for rate limiting** depends on where Medusa
  is ultimately hosted (Medusa Cloud vs. another Node host) and whether that
  host sits behind a trusted proxy. The design above assumes the storefront
  forwards a single trusted header computed server-side; this must be
  confirmed once the Medusa host is chosen, per CLAUDE.md's requirement not
  to add a new hosted service without approval.
- **Shared validation between storefront and backend** (email/first-name
  rules) currently has no shared-package mechanism in this monorepo; 2C.2
  needs a decision on how to avoid duplicating
  `apps/storefront/src/lib/newsletter/validation.ts` rules on the backend
  without introducing a new workspace package prematurely.
- No other material conflicts with existing architecture were found.
