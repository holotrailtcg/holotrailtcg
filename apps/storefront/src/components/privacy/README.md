# components/privacy

Reserved for shared consent and privacy UI (cookie banner, consent controls,
privacy preference dialog). These arrive in a later stage.

Rules:

- Reuse `components/ui` primitives (Button, Checkbox, Alert) — do not create
  parallel controls here.
- Square corners and Holo Trail tokens only.
- No analytics or third-party scripts are wired in this stage.
